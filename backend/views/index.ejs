<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Accueil - ESI Évaluation</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gray-100 flex items-center justify-center">
    <div class="text-center">
        <h1 class="text-3xl font-bold mb-6">Bienvenue</h1>
        <% if (!user) { %>
            <a href="/register" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 mr-2">S'inscrire</a>
            <a href="/login" class="bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600">Se connecter</a>
        <% } else { %>
            <% if (user.profil === 'Étudiant') { %>
                <a href="/evaluation" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">Page d'Évaluation</a>
            <% } else if (user.role === 'Admin') { %>
                <a href="/dashboard" class="bg-indigo-500 text-white px-4 py-2 rounded hover:bg-indigo-600">Tableau de Bord</a>
            <% } %>
            <a href="/logout" class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600">Déconnexion</a>
        <% } %>
    </div>
</body>
</html>